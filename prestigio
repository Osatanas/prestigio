-- Intercepta RemoteFunction que verifica prestígio
local rs = game:GetService("ReplicatedStorage")
local remote = rs:FindFirstChild("CheckPrestigeEligibility")

if remote then
    hookfunction(remote.InvokeServer, function(...)
        return true -- finge que você está elegível
    end)
    print("🧨 Prestígio liberado!")
else
    print("❌ RemoteFunction não encontrado.")
end
